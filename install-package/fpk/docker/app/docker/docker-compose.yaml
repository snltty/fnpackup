version: "3.8"

services:
  fnpackup:
    image: snltty/fnpackup
    container_name: fnpackup
    privileged: true
    network_mode: host
    restart: always
    tty: true
    stdin_open: true
    volumes:
      - "/$TRIM_APPDEST_VOL/@appshare/fnpackup-docker/projects:/app/projects"
      - "/usr/local/bin/appcenter-cli:/app/appcenter-cli:ro"
      - "/usr/local/bin/fnpack:/app/fnpack:ro"